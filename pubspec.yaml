name: safepulse
description: SafePulse – SOS Emergency Alert & Check-in MVP
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^12.0.0
  permission_handler: ^11.3.0
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.3
  uuid: ^4.5.1
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
